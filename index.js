const track = document.querySelector(".carousel_track");
const slides = Array.from(track.children);
const nextButton = document.querySelector(".carousel_btn_left");
const prevButton = document.querySelector(".carousel_btn_right");

const dotsNav